using APRenderer;
using UnityEngine;

[RequireComponent(typeof(RendererMgr))]
public class APSys : MonoBehaviour
{
    private void Start()
    {

    }

    private void Update()
    {
        if (Input.GetKeyDown(KeyCode.Space))
        {

        }
    }
}
